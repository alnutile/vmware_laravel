APP_ENV={{ app_env }}
{% for name, value in env_vars %}
{% if name not in item.exclude or item.exclude[name] == None %}
{{ name }}={{ value }}
{% else %}
{{ name }}={{ item.exclude[name] }}
{% endif %}
{% endfor %}
